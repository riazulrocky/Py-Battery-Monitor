# 🔋 Py Battery Monitor

**Py Battery Monitor** is a simple Python program that lets you check your system's battery status in real-time.  
It shows battery percentage, whether the power is plugged in, and the estimated remaining battery time.

> Works on **Windows, macOS, and Linux**.

---

## ✨ Features

| Feature                        | Description                                      |
|--------------------------------|--------------------------------------------------|
| 🔋 Battery Percentage           | Displays current battery percentage             |
| ⚡ Power Status                  | Shows if the system is plugged in or running on battery |
| ⏱ Remaining Battery Time        | Estimates remaining time in `HH:MM:SS` format   |
| 💻 Cross-Platform Support       | Compatible with Windows, macOS, and Linux      |

---

## 🛠 Dependencies

- Python 3.x
- `psutil` library

Install `psutil` using pip:

```bash
pip install psutil
